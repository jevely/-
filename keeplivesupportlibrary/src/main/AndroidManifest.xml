<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="keeplive.je.com.keeplivesupportlibrary">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true">

        <service android:name=".JobKeepService" />
        <service android:name=".NotifyKeepService" />
        <service android:name=".NotifyKeepService$InnerService" />
        <service android:name=".NotifyJavaService" />
        <service android:name=".NotifyJavaService$InnerService" />



        <service android:name=".two.LocalService" />
        <service
            android:name=".two.RemoteService"
            android:process=":remote" />

    </application>
</manifest>
